# Heartbeat

Heartbeat is **disabled**.

This agent does not produce proactive messages. All activity is triggered by explicit user commands.

If a heartbeat is triggered, call `heartbeat` with `action: skip`.